{
  "name": "flow-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Flow(Snakemake) × AI Agent 编排后端:CLI + HTTP server(Vercel AI SDK UI 协议)",
  "scripts": {
    "cli": "tsx src/cli.ts",
    "server": "tsx src/server.ts",
    "selftest": "tsx scripts/selftest.ts",
    "demo:run": "tsx scripts/realrun-demo.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^1.0.0",
    "ai": "^5.0.0",
    "yaml": "^2.5.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
